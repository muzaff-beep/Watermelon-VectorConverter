# Copyright (c) 2026 Suhail Muzaffari. All rights reserved.
# CI pipeline: lint -> verify -> test -> build -> fidelity (Doc 8 §1.2).
name: verify
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: JNI signature parity (Contract C-3)
        run: python3 ci/verify_jni_parity.py
      # TODO(I): rustfmt/clippy/ktlint/eslint
      # TODO(I): cargo test -p svg-converter-core   (R + P: 22 tests)
      # TODO(I): cargo test -p wvgc-desktop          (B Tauri: 5 tests)
      # TODO(I): cargo-ndk cross-compile arm64-v8a, armeabi-v7a
      # TODO(I): on-device instrumented JNI round-trip test (B)
      # TODO(I): SSIM fidelity gate ; integration harness ; bundle + size asserts
